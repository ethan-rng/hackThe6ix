/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['lh3.googleusercontent.com', 's.gravatar.com'], // Add the domain of your external image source
      },
};

export default nextConfig;
